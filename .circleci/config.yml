version: 2
jobs:
  build:
    docker:
      - image: circleci/node:12.11.1
        environment:
          CXX: g++-6
    steps:
      - checkout
      - run:
          name: coverage
          command: npm run ci-coverage
